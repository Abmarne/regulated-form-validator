# 📦 Regulated Form Validator (React)

YAML-driven, dynamic form validation and rendering for React. Define fields and rules in YAML/JSON, render forms automatically, and enforce compliance-ready validations for BFSI, healthcare, and more.

## 🚀 Features
- **Declarative config:** YAML/JSON for fields and rules.
- **Dynamic rendering:** React form auto-generated from config.
- **Rich validations:** required, regex, length, enum, number, date, crossField, conditional (when), custom.
- **BFSI rules:** PAN, IFSC, Aadhaar built-in.
- **Auditor-friendly:** Human-readable config files.

## 📥 Installation
```bash
npm install regulated-form-validator